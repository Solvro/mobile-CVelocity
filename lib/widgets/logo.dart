import 'package:flutter/material.dart';

class CVLogo extends StatelessWidget {
  const CVLogo({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      width: 100,
      height: 50,
      child: Placeholder(),
    );
  }
}
